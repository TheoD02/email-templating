{% extends 'base.html.twig' %}

{% block title %}EmailTemplate{% endblock %}

{% block body %}
    <h1>EmailTemplate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ email_template.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ email_template.content }}</td>
            </tr>
            <tr>
                <th>AvailableFields</th>
                <td>{{ email_template.availableFields ? email_template.availableFields|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_email_template_index') }}">back to list</a>

    <a href="{{ path('app_email_template_edit', {'id': email_template.id}) }}">edit</a>

    {{ include('email_template/_delete_form.html.twig') }}
{% endblock %}
